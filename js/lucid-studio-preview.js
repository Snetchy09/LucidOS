import { getFiles } from "./filesystem.js";
import { getActiveProject } from "../apps/lucid-projects.js";
import { runLucidScript } from "../apps/lucid-script-runtime.js";

const state={runtime:null,urls:[]};
function collect(files,path=[]){const out=[];for(const file of files){if(file.type==="folder")out.push(...collect(file.children||[],[...path,file.name]));else out.push({...file,path:[...path,file.name]});}return out;}
function projectFiles(){return ["Pictures","Music","Videos"].flatMap(folder=>collect(getFiles([folder]),[folder]));}
function toUrl(file){if(!file)return"";const blob=file.content instanceof Blob?file.content:new Blob([file.content],{type:file.mimeType||"application/octet-stream"});const url=URL.createObjectURL(blob);state.urls.push(url);return url;}
function escape(text){return String(text??"").replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;");}
function pathOf(asset){return Array.isArray(asset?.path)?asset.path.join("/"):String(asset?.path||"");}
function resolveSource(source,files){let code=String(source||"");for(const file of files){if(!file.content)continue;const url=toUrl(file);const path=pathOf(file);for(const value of [path,`Home/${path}`,`/${path}`])code=code.split(value).join(url);}return code;}
function stop(){if(state.runtime?.stop){try{state.runtime.stop()}catch{}}state.runtime=null;state.urls.splice(0).forEach(url=>URL.revokeObjectURL(url));}
function insertSnippet(root,text){const editor=root.closest(".lucid-studio")?.querySelector("#lucid-code");if(!editor)return;const start=editor.selectionStart,end=editor.selectionEnd;const value=editor.value;editor.value=value.slice(0,start)+text+value.slice(end);editor.selectionStart=editor.selectionEnd=start+text.length;editor.dispatchEvent(new Event("input"));editor.focus();}
function openLivePreview(root){const project=getActiveProject();if(!project)return;stop();const overlay=document.createElement("div");overlay.className="lucid-live-preview-overlay";overlay.innerHTML=`<div class="lucid-live-preview" role="dialog" aria-modal="true"><header><div><span>LIVE PREVIEW</span><h2>${escape(project.name)}</h2><p>Run the actual Lucid Script app while working with its project assets.</p></div><button data-close aria-label="Close preview">×</button></header><div class="lucid-live-preview-body"><aside><div class="live-preview-section-title">Project assets</div><div class="live-preview-assets"></div><div class="live-preview-hint">Use an asset path inside <code>image</code> or <code>sprite src</code>. Preview rewrites the path to the local asset automatically.</div></aside><main><div class="live-preview-toolbar"><span data-status>Starting preview…</span><button data-reload>Reload</button></div><div class="live-preview-host"></div></main></div></div>`;document.body.appendChild(overlay);const host=overlay.querySelector(".live-preview-host"),assets=overlay.querySelector(".live-preview-assets"),status=overlay.querySelector("[data-status]"),files=projectFiles();const close=()=>{stop();overlay.remove()};overlay.querySelector("[data-close]").addEventListener("click",close);overlay.addEventListener("click",event=>{if(event.target===overlay)close()});const render=()=>{stop();host.innerHTML="";status.textContent="Running…";try{const source=resolveSource(project.source,files);state.runtime=runLucidScript(source,host,{permissions:[]});status.textContent=`Running ${state.runtime.appName||project.name}`}catch(error){status.textContent="Preview failed";host.innerHTML=`<div class="live-preview-error"><strong>Runtime error</strong><span>${escape(error.message||error)}</span></div>`}};overlay.querySelector("[data-reload]").addEventListener("click",render);if(!files.length)assets.innerHTML='<div class="live-preview-empty">No assets in Pictures, Music or Videos.</div>';else files.forEach(file=>{const row=document.createElement("div");row.className="live-preview-asset";const path=pathOf(file);const isImage=(file.mimeType||"").startsWith("image/")||/\.(png|jpe?g|webp|gif)$/i.test(file.name);const isAudio=(file.mimeType||"").startsWith("audio/")||/\.(mp3|wav|ogg|m4a)$/i.test(file.name);row.innerHTML=`<div><strong>${escape(file.name)}</strong><small>${escape(path)}</small></div><div class="live-preview-asset-actions">${isImage?'<button data-image>Insert image</button>':''}${isImage?'<button data-sprite>Insert sprite</button>':''}${isAudio?'<button data-audio>Play</button>':''}</div>`;if(isImage){row.querySelector("[data-image]")?.addEventListener("click",()=>insertSnippet(root,`image "${path}"\n`));row.querySelector("[data-sprite]")?.addEventListener("click",()=>insertSnippet(root,`sprite "${file.name.replace(/\.[^.]+$/,'')}" {\n    x 100\n    y 100\n    width 64\n    height 64\n    src "${path}"\n}\n`));}if(isAudio)row.querySelector("[data-audio]")?.addEventListener("click",()=>{const audio=new Audio(toUrl(file));audio.addEventListener("ended",()=>URL.revokeObjectURL(audio.src),{once:true});audio.play().catch(()=>{status.textContent="Browser blocked audio until interaction."})});assets.appendChild(row)});render();}
document.addEventListener("click",event=>{const button=event.target.closest(".lucid-studio-preview-button");if(!button)return;event.preventDefault();event.stopImmediatePropagation();const root=button.closest(".lucid-studio");if(root)openLivePreview(root)},{capture:true});
const observer=new MutationObserver(()=>document.querySelectorAll(".lucid-studio-preview-button").forEach(button=>{button.textContent="Live Preview";button.title="Run the actual project with its assets"}));observer.observe(document.body,{childList:true,subtree:true});